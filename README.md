# Spoon_Language
Simple Python script that prints an ASCII spoon and encodes/decodes text using a “Spoon Language.” Mode 1 inserts “v” between doubled vowels (a → ava). Mode 2 reverses it back to normal text. Takes user input and processes characters step by step.
